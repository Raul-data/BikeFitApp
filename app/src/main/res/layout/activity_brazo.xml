<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- ==========================================================
         CAPA DE FONDO
         FrameLayout se usa como contenedor raíz para poder superponer
         vistas (por ejemplo, fondo + contenido principal).
         ========================================================== -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#2E6592" />
    <!-- Fondo azul principal del layout -->

    <!-- ==========================================================
         CONTENIDO PRINCIPAL
         LinearLayout vertical que organiza todos los elementos
         (título, imagen, texto, SeekBar y botón).
         ========================================================== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        android:gravity="center_horizontal">
        <!-- 'gravity' centra horizontalmente todo su contenido -->

        <!-- ======================================================
             TÍTULO
             Muestra el nombre de la medida que el usuario ajusta.
             ====================================================== -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Medida Brazo"
            android:textColor="#FFFFFF"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="16dp" />

        <!-- ======================================================
             ILUSTRACIÓN DEL CUERPO
             Muestra una imagen representativa de la parte del cuerpo.
             Se encuentra dentro de un FrameLayout para centrarla fácilmente.
             ====================================================== -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginBottom="16dp">

            <!-- Imagen del brazo -->
            <ImageView
                android:id="@+id/img_ilustracion"
                android:layout_width="200dp"
                android:layout_height="300dp"
                android:layout_gravity="center"
                android:src="@drawable/bc_brazo"
                android:scaleType="fitCenter"
                android:alpha="0.7" />
            <!-- 'alpha' reduce opacidad para que no distraiga demasiado -->
        </FrameLayout>

        <!-- ======================================================
             DESCRIPCIÓN
             Indica al usuario cómo debe tomarse la medida.
             ====================================================== -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Estando de pie sin calzado, apoyado en una pared, sitúe un objeto entre las dos piernas como si fuera un sillín de bicicleta, eleve dicho objeto desde la parte superior de este objeto hasta el suelo. Escribe la medida y pulsa guardar."
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:gravity="center"
            android:padding="16dp"
            android:background="#4D000000"
            android:layout_marginBottom="16dp" />
        <!-- Fondo semitransparente para resaltar el texto -->

        <!-- ======================================================
             VALOR DE LA MEDIDA
             Muestra el número actual (en cm) según la SeekBar.
             Este ID se enlaza con textValorMedida en Kotlin.
             ====================================================== -->
        <TextView
            android:id="@+id/text_valor_medida"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="35 cm"
            android:textColor="#FFFFFF"
            android:textSize="32sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <!-- ======================================================
             SEEK BAR (Selector de medida)
             Permite ajustar la medida del brazo.
             Se enlaza con 'seekbarMedida' en el código Kotlin.
             Rango total (ajustado desde Kotlin)
             Posición inicial
             Color de progreso
             Color del control deslizant
             ====================================================== -->
        <SeekBar
            android:id="@+id/seekbar_medida"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="40"
            android:progress="15"
            android:progressTint="#00BCD4"
            android:thumbTint="#00BCD4"
            android:progressBackgroundTint="#FFFFFF"
            android:layout_marginBottom="24dp"
        android:layout_marginHorizontal="24dp" />

        <!-- ======================================================
             BOTÓN GUARDAR
             Devuelve el valor seleccionado a la actividad anterior.
             Se enlaza con 'btnGuardar' en el código Kotlin.
             ====================================================== -->
        <Button
            android:id="@+id/btn_guardar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Guardar"
            android:textColor="#000000"
            android:backgroundTint="#00BCD4"
            android:paddingHorizontal="48dp"
            android:paddingVertical="12dp"
            android:textSize="16sp"
            android:textStyle="bold"
            app:cornerRadius="25dp"
            android:layout_marginBottom="32dp" />

    </LinearLayout>

</FrameLayout>
